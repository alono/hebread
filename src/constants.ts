/** On-screen logo — always fully niqqud-ed Hebrew (PRD §intro, CLAUDE.md). */
export const LOGO = 'קוֹרֵא עִבְרִית';

/** localStorage key for persisted progress, with version for migrations. */
export const STORAGE_KEY = 'hebread-progress-v1';
